# 実装ワークフローと学び

このリポジトリは「Excel的なグリッドエディタを、後から手を入れやすい形で作る」ための
実験として書かれた。成果物そのものより、**どういう順序で作り、何をどう検証したか**に
主眼がある。ここではその工程と、実際にやってみて分かったことを記録する。

対象期間: 2026-08-07 〜 08。コミットは4本(`2b33478` → `9323e9c` → `978fe05` → `20e32b3`)。
テストは 29件 → 41件に増えた。

---

## 1. 採用した工程

```
モデル(型) → 不変条件 → アクション → reducer → テスト → レンダラ
                 ↑                                ↑
            Alloy で設計探索              property-based test で
            (spec/grid.als)              実装そのものをモデル検査
```

UIから状態を切り離し、**状態遷移を純関数に閉じ込めてから描画を作る**。
グリッドエディタの複雑さの本体は「結合セルと選択範囲の整合性」「編集モードの
状態遷移」「undo/redo」といった状態管理であって、描画はそこから従属的に決まる。

各工程の成果物は README.md のアーキテクチャ節を参照。

### 1.1 当初案からの変更点

最初の構想は `モデル → Alloy → アクション → reducer → テスト → TLA+ → レンダラ` だった。
議論の結果、**TLA+ を落として property-based test に置き換えた**。判断の理由:

- TLA+ が真価を発揮するのは並行性・非同期性のある系。この reducer は
  **単一ユーザー・決定的・同期的**で、共同編集も入れない。TLA+ による検証は実質
  「全アクション列の網羅テスト」であり、それならもっと軽い手段がある。
- 形式仕様は**実装の写し**であって実装そのものではない。検証済みモデルと実装が
  ずれると保証が実装に及ばない。CI で回せないのも痛い。
- fast-check でランダムなアクション列を**実 reducer** に流し、毎ステップ不変条件を
  assert すれば、事実上「実装に対するモデル検査」になる。乖離が原理的に起きない。

**共同編集(CRDT/OT)を入れるなら判断は逆になる。** そこは TLA+ を強く推せる領域。

Alloy は「設計初期の思考ツール」として残した。`spec/grid.als` は
`src/model/invariants.ts` の I1〜I7 と対応させてあり、設計変更時に
「この不変条件の組で変なインスタンスが作れないか」を探索できる。
**TS 側が正**で、Alloy は捨てても実装が壊れない位置に置いてある。

### 1.2 効いた設計判断: 安定IDベース

行・列を配列インデックスではなく安定ID(`r12` / `c34`)で持ち、表示位置は
配列順から毎回導出する。セル・結合矩形の角・数式の参照はすべてIDで紐づく。

**この判断の効果は数式で最も大きく出た。** 行を挿入しても `=A3` の参照は
自動追従し、数式バーの表示だけが `=A4` に変わる。インデックスベースなら
全数式の参照書き換えが必要になっていた。

副作用として「角のIDが消えると矩形全体が壊れる」問題が後で顕在化した(§5.2)。
角2点だけを持つ表現は、角が削除される場合の**寄せ直し**とセットで初めて成立する。

---

## 2. 不変条件を先に書く

`src/model/invariants.ts` に I1〜I10 を述語関数として明文化した。

```ts
// 例
// I6: 結合矩形どうしは重ならない
// I7: 結合の非アンカー被覆セルは内容を持たない
// I9: 選択の anchor/focus は存在するセルで、被覆セルを指さない
```

これを先に書いたことで **reducer の契約が明確になった**:

1. 引数を一切ミューテートしない
2. 不正な引数(存在しないID、範囲外の値)でも状態を壊さず no-op
3. doc が変化するアクションのみ履歴に積む

契約2があるおかげで、property test のジェネレータは**雑な値を投げてよい**。
存在しないID・範囲外インデックス・負のサイズを混ぜて生成し、堅牢性そのものを
検証対象にできる。

---

## 3. property-based test で何を検証したか

`src/model/properties.test.ts` の4つの性質:

| | 性質 |
|---|---|
| P1 | 任意のアクション列で不変条件 I1〜I10 が全ステップ保たれる |
| P2 | reducer は引数をミューテートしない(deep-freeze で検出) |
| P3 | 全 undo で履歴が正確に巻き戻り、全 redo で完全復元される(**参照一致**) |
| P4 | 数式評価は例外を投げず、同一 doc に対して決定的 |

### 検出力を別途確認した

property test は「常に緑」だと機能しているのか分からない。以下を別途確認した:

- `fc.assert` が指定回数(300回)実際に実行されていること
- 壊した状態(`rows: []`)を不変条件チェッカが検出すること
- deep-freeze がミューテーションを実際に捕まえること

**この確認をやらないと「テストが何も見ていない」状態に気づけない。**

---

## 4. 多エージェント並列レビュー(/simplify)

初版commit後、4つの観点(Reuse / Simplification / Efficiency / Altitude)で
独立したレビューエージェントを並列起動した。27指摘 → 重複排除して15件修正。

### 学び: 独立した一致が最強の信号

**3つのエージェントが独立に同じ重複を指摘した**箇所があった
(結合矩形の固定点拡張が `doMerge` と `selectionRect` に二重実装)。
観点が違うのに同じ場所を指すのは、そこが本当に構造的な問題である強い証拠。

### 副産物: バグ予備軍が見つかった

Altitude 観点のエージェントが「同じ機構が二重実装されているだけでなく、
**それを使うべき3つ目の呼び出し側が使っていない**」ことを指摘した。
実際 `clearRange` は選択ハイライトと**違う範囲**を消していた。
コードの重複を指摘する過程で、仕様の不整合が浮かんだ形。

同様に「reducer が編集中の内容を黙って破棄する経路がある」というデータロス
予備軍も見つかった。これは重複の指摘ではなく「編集ライフサイクルが3層に
分散していて単一の所有者がいない」という altitude の指摘から出てきた。

---

## 5. AIによる疑似ユーザテスト(予測→実行→差分)

**このプロジェクトで最も収穫が大きかった手法。**

### 5.1 プロトコル

実装を一切知らないエージェントに、スクリーンショットだけを渡して操作させる。
1操作ごとに以下を厳守させる:

```
PREDICT  操作の「前」に、何をするか・その結果どうなるかを具体的に宣言する
ACT      実行する
OBSERVE  スクリーンショットを撮り、実際に何が起きたかを見たまま書く
VERDICT  MATCH / VIOLATION を判定し、困り度を書く
```

**制約が本質:**

- ソースコードを Read/Grep/Bash で見ることを禁止
- DOM・console の参照を禁止(`read_page` / `javascript_tool` / `get_page_text` など)
- **スクリーンショットのみ**を情報源にする(人間の被験者は画面しか見えない)
- 評価側は必ず**別セッション**。同じ会話コンテキストのAIに評価させると自己肯定バイアスがかかる

**予測を先に書かせるのが肝。** 後付けの感想ではなく、期待と現実の差分が記録される。
予測が**当たった**記録も価値がある(「安定IDによる参照追従はユーザーの直感と合っている」
という積極的な確認になった)。

### 5.2 実際に見つかったもの

第1回(Excel熟練者ペルソナ)で5件の指摘。**裏取りの結果、4件が実バグ、1件は偽陽性。**

| 指摘 | 判定 | 原因 |
|---|---|---|
| 日本語(IME)で打ち始めても編集が始まらない | 実バグ(致命的) | keydown の `key` は IME 有効時 `"Process"`。押されたキーから編集を開始する方式では**原理的に**日本語を拾えない |
| 行・列ヘッダのクリックで選択できない | 実バグ | `fill="none"` の SVG 矩形は内側がヒットテスト対象外。ハンドラは正しいのに枠線1pxにしか当たっていなかった |
| 数式バーが編集できない | 実バグ | 見た目は Excel の数式バーなのに実体は表示専用テキスト |
| Alt+Enter でセル内改行できない | 実バグ | Shift+Enter に割り当てていた(Excel と逆) |
| 行の高さを変える手段が存在しない | **偽陽性** | 実際は動く。掴み代が5pxで掴み損ねていた |

第2回(修正版の再テスト、盲検)では**前回の6件すべてが MATCH** となり、修正の
有効性を第三者視点で確認できた。同時に新規2件を検出:

- **Shift+クリックの範囲選択が未実装**(致命的)
- **SUM のレンジが端の行削除で `#REF!` になる** — Excel は `SUM(A1:A3)` の1行目を
  消すと `SUM(A1:A2)` に縮む。角のIDだけを持つ表現の穴(§1.2)

第3回(表計算未経験ペルソナ)は**熟練者と全く違う欠陥**を見つけた:

- 合計の出し方への導線が実質ゼロ(Σボタンも右クリックメニューもなく、
  唯一の手がかりが画面最下部の薄い注意書き一行)
- `#REF!` と「結合」というラベルの意味が最後まで分からない
- 保存・書き出しが見当たらず、作った表がどこに残るのか不明

**ペルソナを変えると見える欠陥が変わる。** 熟練者は*操作モデルの穴*を、
初見ユーザーは*発見可能性の欠如*を見つけた。指摘はほぼ重複しなかった。

### 5.3 必ず裏取りする

**エージェントの「動きません」は「私には動かせませんでした」と読む。**
5件中1件が操作失敗による偽陽性だった。裏取りを挟まずに修正に入ると、
**動いている機能を壊しに行く**ことになる。

ただし偽陽性も無価値ではない。「5pxの掴み代は熟練者でも掴み損ねる」という
発見性の問題は実在した。「機能がない」ではなく「見つけられない」が正しい結論。

### 5.4 自動化ツール由来の計測ノイズ

エージェントが使うブラウザ自動化は人間より桁違いに速く打つため、
**アプリの不具合ではない現象**が混ざる:

- `navigate` 直後にキー入力を送ると、ページが対話可能になる前に打ち込むことになり
  文字が落ちる。**温間では毎回成功し冷間では失敗する**という対比で切り分けた
- 一度に大量の文字を送ると、再描画が追いつかず取りこぼしに見えることがある

対策としてエージェントのプロンプトに以下を入れた(これは**計測上の注意**であって
実装のヒントではないので、盲検性を損なわない):

- `navigate` 後に4秒以上待ち、捨て打ち1文字で反応を確認してから本番に入る
- 1回の `type` は10文字程度まで
- 取りこぼしたら**落ち着いてやり直して再現するか**を確かめ、再現しないものは報告しない

---

## 6. いちばん大きな学び: テストが守れない層がある

41件のテストと property test は**モデル層を完全に守った**。この層のバグは1件も
ユーザテストに漏れていない。一方、以下は**テストが緑のまま壊れていた**:

| 層 | 実際に起きたこと |
|---|---|
| SVG のヒットテスト | `fill="none"` で内側がクリック不能。後に `pointerEvents="all"` を足したら、今度は**隣接ヘッダがリサイズ掴み代の下半分を覆った** |
| 描画順序 | 掴み代がヘッダ矩形より前に描かれていて奪われる。`elementsFromPoint` で重なりを見て初めて分かった |
| フォーカス | グリッドの `mousedown` 既定動作でフォーカスが body へ逃げ、常設エディタがキー入力を受け取れない |
| IME | keydown に文字が載ってこない。keydown ベースの設計が**原理的に**成立しない |
| React の再描画競合 | 入力欄を制御コンポーネントにしたら、高速入力時に古い value が DOM を巻き戻して文字が落ちた |

**このうち2件は、私が修正の過程で自分で入れた回帰だった。**
どちらもテストは全部緑のままで、ブラウザで実際に触るまで気づけなかった。

### 帰結

- モデル層は property-based test で守る。ここは費用対効果が非常に高い
- **描画・入力・フォーカス層はブラウザ実機での確認が必須**。ここを自動テストで
  守ろうとするより、疑似ユーザテストを回す方が現実的だった
- 「テストが緑」は「モデルが正しい」であって「アプリが動く」ではない

---

## 7. 再利用するならこの順で

1. **型でモデルを書く。** 座標は安定IDか、インデックス+書き換えかを最初に決める。
   数式・参照・結合など「位置を指すもの」があるなら安定IDが強い
2. **不変条件を述語関数として書く。** Alloy は設計探索に数時間使うのはあり。
   ただし成果物は TS に翻訳し、Alloy は捨てても壊れない位置に置く
3. **アクションを定義し、「不正な引数は no-op」を契約にする。**
   これがあると property test のジェネレータが雑でよくなる
4. **reducer を純関数で書く。** 編集の確定・破棄のような
   *規則*は reducer に所有させる。ビューに散らすとデータロス経路ができる
5. **property-based test を書き、検出力を別途確認する**(§3)
6. **レンダラを作る**
7. **多エージェント並列レビューをかける**(Reuse / Simplification / Efficiency / Altitude)。
   独立した一致を優先的に直す
8. **実装非開示の疑似ユーザテストを、複数ペルソナで回す。**
   指摘は必ず実機で裏取りし、実バグと操作失敗を切り分ける
9. **修正したら同じペルソナで盲検の再テスト**をかけ、解消を第三者視点で確認する

7〜9 は1回で終わらせず、修正のたびに回す。第2回で新規2件、第3回で別種の欠陥が
出たように、**アプリが変わると露出する欠陥も変わる**。

---

## 8. 未対応として残っている設計判断

以下は実在を確認済みだが、設計方針の変更を伴うため保留した。

- **行を削除するとシート自体が縮む**(20行→17行)。Excel は行数固定。
  末尾に自動補充するか、初期行数を大きくして仮想化するかの判断が要る
- **編集中のセルが1行分の高さのまま**。自動行高が確定済み内容から計算されるため。
  下書きも高さ計算に含めれば直る
- **undo で選択が元の位置に戻らない**。選択は ID を追いかけるので、
  復元された行が別の位置にあると選択もそこへ行く。Excel は削除前の選択を復元する。
  変えるなら「選択を undo 履歴に含めない」という当初の設計判断の見直しになる
- **初心者向けの導線**(合計ボタン、右クリックメニュー、エラーの説明)は機能追加
